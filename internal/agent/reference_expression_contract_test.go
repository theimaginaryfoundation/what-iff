package agent

import "testing"

// Phase 1 regression contract. The implementation must make these methods real:
// a reference-conditioned request can only be reported when the provider both
// declares support and actually receives canonical image data.
func TestReferenceExpressionGenerationContract(t *testing.T) {
	t.Skip("implementation pending: replace with executable provider/request assertions")
}
